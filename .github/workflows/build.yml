name: Build N64 ROM

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Debug libdragon paths
      run: |
        docker run --rm ghcr.io/dragonminded/libdragon:latest sh -c "echo N64_INST=\$N64_INST && ls -la \$N64_INST 2>/dev/null || echo 'N64_INST not set or invalid' && find / -name 'n64.mk' 2>/dev/null | head -5"

    - name: Build ROM with libdragon Docker
      run: |
        docker run --rm \
          --mount type=bind,source=$(pwd),target=/project \
          --workdir=/project \
          ghcr.io/dragonminded/libdragon:latest \
          make -f Makefile.n64 clean all
        ls -la *.z64 || echo "ROM not created"

    - name: Upload ROM artifact
      uses: actions/upload-artifact@v4
      with:
        name: neuron-rom
        path: neuron.z64
        if-no-files-found: error
        retention-days: 30

    - name: Create Release (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: neuron.z64
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
